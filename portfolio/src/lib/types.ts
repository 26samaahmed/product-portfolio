export type Project = {
  title: string;
  project_duration: string;
  role: string;
  problem_statement: string;
  imageUrl: string;
  tags: string[];
};

export type Button = {
  name: string;
  link: string;
}